# Quota Blocks 个人修改总结

这份记录总结了本次围绕 Windows 版本 Quota Blocks 的部署、界面调整和最终归档内容。

## 1. 初始部署

- 下载并整理了 `NathanCheng685/quota-blocks` 项目。
- 完成 Windows 版本的依赖检查、编译、发布和启动。
- 使用 .NET 8 完成 Windows 程序构建。
- 程序安装位置：`%LOCALAPPDATA%\Programs\QuotaBlocks`。
- 设置了 Windows 开机自动启动。
- 验证程序能够正常读取额度并在任务栏运行。

## 2. 任务栏显示调整

- 将额度显示放到 Windows 任务栏靠近系统小组件的位置。
- 调整横向位置，避免与 Windows 自带天气和小组件区域重叠。
- 根据安装和可用状态动态显示任务栏行：只安装 ChatGPT / Codex 或只安装 Claude 时，只显示对应程序的额度；Claude 未安装或不可用时自动隐藏对应行。
- 单 provider 显示时自动使用更大的视觉比例。
- 增大任务栏字体、图标和额度区块，使其更容易阅读。
- 任务栏中的品牌 logo 统一使用白色显示。
- 额度区块和百分比采用类似手机额度的颜色规则：
  - 80%～100%：绿色；
  - 20%～79%：黄色；
  - 0%～19%：红色。

## 3. 详细面板调整

- 增大详细面板中的标题、额度和菜单文字。
- 统一 ChatGPT / Codex 与 Claude 的标题字号和字重。
- 统一两个 provider 图标的可见尺寸和垂直位置。
- 调整“周额度、剩余、重置时间”的字号，使其清楚易读，同时小于标题。
- 缩小标题与周额度之间不必要的空隙。
- 重新计算面板宽度，避免菜单文字强行撑宽面板。
- 根据额度行和最长菜单行自适应宽度，并保留足够的右侧空间，避免文字和勾选标记被截断。
- 保留“开机自动启动”菜单项，并将勾选标记放在右侧。
- 保留 Codex 和 Claude 额度页面入口，以及中英文切换和退出功能。

## 4. 额度颜色示例

任务栏剩余额度使用类似手机电量的颜色规则：80%～100% 为绿色，20%～79% 为黄色，0%～19% 为红色。

![三种额度颜色状态](windows/docs/quota-color-states.png)

## 5. 原程序与修改后对比

| 原程序任务栏 | 个人修改后任务栏 |
| --- | --- |
| ![原程序任务栏](windows/docs/bar.png) | ![修改后任务栏](windows/docs/taskbar-current.png) |

## 6. 验证结果

- Windows 项目编译通过。
- 编译结果：0 个警告、0 个错误。
- 程序已发布并运行于本机安装目录。
- 最终面板和任务栏截图已保存到 `windows/docs/`。
- 源码、中文说明和截图已提交到 GitHub 的 `quota-blocks-windows-final` 分支。

## 7. 版权说明

本次修改是基于 [NathanCheng685/quota-blocks](https://github.com/NathanCheng685/quota-blocks) 的个人定制版本，主要服务于个人 Windows 使用需求。

原项目的版权、作者署名、代码资源和 MIT 许可证仍归 NathanCheng685 所有。本次修改不改变原许可证要求，也不主张拥有原项目的版权。再使用或再发布时，应继续保留原项目的版权声明和许可证内容。
